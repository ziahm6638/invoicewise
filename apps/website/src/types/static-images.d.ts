// Next's generated `next-env.d.ts` is gitignored, so a fresh clone has no
// static-image module declarations until `next dev`/`next build` runs. These
// references keep `tsc --noEmit` independent of that generated file.
/// <reference types="next" />
/// <reference types="next/image-types/global" />
